App.jsx
├── <Navbar />          ← conditionally rendered (hidden on /login, /signup)
│   └── Right section:
│       ├── IF not logged in → [LogIn] [SignUp] buttons
│       └── IF logged in → [👤 Username] profile icon
└── <Routes>
    ├── /           → <Home />
    ├── /aboutUs    → <AboutUs />
    ├── /contact    → <Contact />
    ├── /login      → <Login />      ← NO navbar here ✅
    └── /signup     → <SignUp />     ← NO navbar here ✅
            What                                                     	How                                            	File
Hide navbar on login/signup                           	useLocation() + conditional rende                           r	App.jsx
Show profile instead of buttons                           	useAuth() + ternary in JSX                            	navbar.jsx
Redirect after login                            	useNavigate() + login() context call                            	Your Login/SignUp components
Persist across refresh                            	Store JWT in localStorage, check on mount                           	AuthContext.jsx (future step)